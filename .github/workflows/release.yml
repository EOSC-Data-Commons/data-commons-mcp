name: Release
on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build:
    name: 📦 Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: rustup update
      - run: cargo install cargo-deny
      - run: cargo fmt -- --check
      - run: cargo clippy --all --all-targets --all-features
      - run: cargo deny check
      - run: cargo build --release

  docker:
    name: 🐳 Docker
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t data-commons-mcp .

      # - name: Test Docker image
      #   run: |
      #     CONTAINER_ID=$(docker run -d -p 8000:8000 data-commons-mcp)
      #     echo "Started container: $CONTAINER_ID"
      #     sleep 5
      #     docker stop $CONTAINER_ID
      #     docker rm $CONTAINER_ID

  generate-changelog:
    name: 🏔️ Generate changelog for GitHub release
    runs-on: ubuntu-latest
    outputs:
      release_body: ${{ steps.git-cliff.outputs.content }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate a changelog
        uses: orhun/git-cliff-action@main
        id: git-cliff
        with:
          config: cliff.toml
          args: -vv --latest --strip header
        env:
          OUTPUT: CHANGES.md

      - name: Release
        uses: softprops/action-gh-release@v2.2.1
        with:
          body_path: CHANGES.md
        if: startsWith(github.ref, 'refs/tags/')

  # release:
  #   name: Build Release Binaries
  #   runs-on: ${{ matrix.os }}
  #   strategy:
  #     matrix:
  #       include:
  #         - os: ubuntu-latest
  #           target: x86_64-unknown-linux-gnu
  #           artifact_name: data-commons-mcp
  #           asset_name: data-commons-mcp-linux-amd64
  #         - os: ubuntu-latest
  #           target: aarch64-unknown-linux-gnu
  #           artifact_name: data-commons-mcp
  #           asset_name: data-commons-mcp-linux-arm64
  #         - os: macos-latest
  #           target: x86_64-apple-darwin
  #           artifact_name: data-commons-mcp
  #           asset_name: data-commons-mcp-macos-amd64
  #         - os: macos-latest
  #           target: aarch64-apple-darwin
  #           artifact_name: data-commons-mcp
  #           asset_name: data-commons-mcp-macos-arm64
  #   steps:
  #     - uses: actions/checkout@v5

  #     # - name: Install Rust
  #     #   uses: dtolnay/rust-toolchain@stable
  #     #   with:
  #     #     targets: ${{ matrix.target }}

  #     - name: Install cross-compilation tools (Linux ARM64)
  #       if: matrix.target == 'aarch64-unknown-linux-gnu'
  #       run: |
  #         sudo apt-get update
  #         sudo apt-get install -y gcc-aarch64-linux-gnu
  #       # echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc" >> $GITHUB_ENV

  #     - name: Build binary
  #       run: cargo build --release --target ${{ matrix.target }}
  #       env:
  #         CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc

  #     - name: Upload artifacts
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: ${{ matrix.asset_name }}
  #         path: target/${{ matrix.target }}/release/${{ matrix.artifact_name }}${{ matrix.os == 'windows-latest' && '.exe' || '' }}
